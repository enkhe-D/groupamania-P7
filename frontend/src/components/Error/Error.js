import React from "react";
import classes from "../Error/Error.module.css";

const Error = () => {
  return <p className={classes.error}>Oups! cette page n'existe pas! </p>;
};

export default Error;
